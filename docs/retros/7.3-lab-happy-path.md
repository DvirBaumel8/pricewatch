# §7.3 Retrospective — Lab happy path (§7.2)

**Status:** Analysis complete (no product rewrite)  
**Date:** 2026-09-22 (Asia/Jerusalem)  
**Author:** Developer Bot (for Carlos)  
**Inputs:** QA PASS report `/workspace/qa-reports/pricewatch-7.2-pr1.md`, evidence `/workspace/qa-reports/evidence-7.2/`, merged PR [#1](https://github.com/DvirBaumel8/pricewatch/pull/1) (`main` @ `f50feb7`; QA ran head `3917a17`)

---

## 1. What ran

| Stage | What happened |
|-------|----------------|
| **Discover** | `node src/discover.js http://127.0.0.1:3847 "main monthly price"` |
| **Ladder step** | Step 1 succeeded — method **`api`** via `GET /price.json` (`discovery_step: 1`) |
| **Skill** | ID `6993b18e3e3d` — `pricing_url` → `/price.json`, `json_path` `$.amount`, normalize currency/period fields, confidence 0.99 |
| **Monitor** | Loads skill → short HTTP fetch → compare snapshot → file sink `outbox/` on change |
| **Sequence (QA)** | Baseline monitor (no email) → unchanged monitor (no email) → `POST /set-price {"amount":39}` → monitor → exactly one email (29→39) → lab reset to 29 |

Proxies: **not used**. No Playwright / DOM fallback needed on lab. No keep-alive browser tabs.

---

## 2. Tokens / cost

| Path | LLM tokens | Notes |
|------|------------|--------|
| **Monitor (Service C path)** | **0** | Required by §7.2 / §7.3; QA confirmed via run log + source grep |
| **Discover (lab)** | **0** | API step only; no LLM client calls in `src/` / `scripts/` |
| **Billed API** | **$0** | No OpenAI/Anthropic usage on this path |

§7.3 pass criterion on tokens: **met** (monitor path 0 LLM tokens; onboarding within caps — lab never left step 1).

---

## 3. Wall time / steps

Formal per-step timers were not instrumented in the product CLI. From QA evidence:

- Official live script: `./scripts/run-7.2.sh http://127.0.0.1:3847` → **exit 0**, all checks green (`evidence-7.2/run-7.2-stdout.txt`).
- Snapshot timestamps around discover/monitor sit within the same second window (`2026-09-21T23:06:07.z` UTC) — wall clock for the core monitor ticks is **sub-second per fetch** on localhost; full script including curl set-price and asserts completed quickly on the shared lab.
- Ladder: **1 HTTP page load** for discover (API); monitor = **1 HTTP GET** per tick.

**Improvement note:** add lightweight duration logs (ms) on discover + monitor for future retros (§7.6 will need this on real sites).

---

## 4. Success vs §7.2 criteria?

**Yes — QA declared PASS.** Cite: [QA report §7.2](../../qa-reports/pricewatch-7.2-pr1.md) (executed 2026-09-22 02:06 IDT).

| Criterion | Result |
|-----------|--------|
| Discover → skill ready | PASS — skill `6993b18e3e3d`, method `api` |
| Unchanged price → no email | PASS — outbox count 0 |
| Change → exactly one email with before/after | PASS — before 29, after 39 |
| Snapshots stored | PASS |
| Monitor 0 LLM tokens | PASS |

False emails / missed change / wrong amount: **none** observed.

---

## 5. Improvements (ranked)

### Must-fix before §7.5 (real competitors)

1. **Duration + step telemetry** — log wall ms and ladder step on every discover/monitor run so §7.6 analysis isn't guesswork.
2. **Idempotent outbox / artifact retention** — official `run-7.2.sh` cleanup trap deletes skills/snapshots/outbox on EXIT; QA had to re-run manually to preserve evidence. Add an `--keep-artifacts` (or copy-to-evidence) mode for QA.
3. **Blocked / challenge detection** — lab never hits anti-bot; real sites will. Ensure empty/blocked responses do not write a false price snapshot (spec §5).

### Nice-to-have

4. **Structured machine-readable summary JSON** at end of `run-7.2.sh` (skill id, email count, tokens, timings) for QA bots to parse without scraping ANSI logs.
5. **DOM path exercise on lab** — optional lab mode that hides `/price.json` so step-2 DOM discovery is regression-tested before real sites.
6. **Email sink schema docs** — document outbox JSON fields for future Mailhog/Resend swap.

### Defer

7. Proxies / Playwright keep-alive policies (not needed until blocks appear).
8. Sub-daily cron intervals (MVP daily only).
9. Real email provider wiring (file sink is correct for lab MVP).

No product code change required for this analysis itself.

---

## 6. Recommendation

| Option | Decision |
|--------|----------|
| Skip §7.4 | **Yes — recommend skip** |
| Do §7.4 re-run | Not required: no must-fix product changes shipped after §7.2 PASS; analysis-only |
| Ready for §7.5 prep | **Yes**, with the must-fix telemetry/artifact/block-detection items tracked as prep work (can land as small follow-up PRs before or alongside first real-case attempts) |

**Rationale:** §7.3 pass = monitor 0 LLM tokens + analysis written with improvements. Happy path quality is solid on controlled lab. §7.4 exists to re-prove after improvements; since we are not changing extractor/cron behavior in this PR, a full 7.4 re-run adds little. Proceed to **§7.5 prep** (pick 3 public pricing pages; ensure caps and block handling are ready).

---

## Proof links

- Merged implementation: https://github.com/DvirBaumel8/pricewatch/pull/1  
- QA PASS report (local ops path): `/workspace/qa-reports/pricewatch-7.2-pr1.md`  
- Evidence dir: `/workspace/qa-reports/evidence-7.2/`
